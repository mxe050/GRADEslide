# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.vscode/
.idea/

# Node / Next.js (web/)
web/node_modules/
web/.next/
web/out/
web/build/
web/.vercel/
web/next-env.d.ts.bak
web/*.log
web/.env*.local
web/coverage/

# Python (pptx_work/)
pptx_work/__pycache__/
pptx_work/*.pyc
pptx_work/.venv/

# Generated/source data — re-buildable from extract.py + build-slides.mjs
pptx_work/extracted_output/
# Rendered slide JPEGs — re-buildable from sample.pptx via PowerPoint COM
pptx_work/rendered_slides/

# Original PowerPoint source — keep local, do not redistribute
pptx_work/sample.pptx

# Original intro images at repo root (Japanese filenames, kept local).
# Note: leading slash anchors to repo root so web/public/images/intro/ is unaffected.
/intro/

# Local helper notes
新しいテキスト ドキュメント.txt

# Ad-hoc temp
*.tmp
.cache/

# slides.json backups created by /api/save-and-push (dev only)
web/public/data/slides.json.bak.*
web/public/data/slides.json.tmp.*

# Locally downloaded merged exports (✎ JSON エクスポート)
slides.edited.*.json
